import type { CustomizationOptions } from "wasp/client/auth";

export const AuthApperance: CustomizationOptions['appearance'] = {
    colors: {
        brand: '#1501FE',
        brandAccent: '#1605E1',
        submitButtonText: 'white',
    }
}